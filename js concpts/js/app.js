import { TaskManager } from "./taskmanager";

const manager = new TaskManager();
manager.addTask("Learn JS Modules");
console.log (maager.tasks);

function completed() {
    manager.addTask("classes", true);
    console.log(manager.task);
}